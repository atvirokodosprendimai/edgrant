---
tldr: First actionable items after eidos bootstrap — no plans/todos exist yet, forward-looking opportunities
category: core
---

# Next: Post-Bootstrap Actionable Items

## Active Plans

None.

## Open Comments

None.

## Planned Items

None.

## Inline Hints (informational, not actionable)

- `{>> JSON for simplicity, Biscuit for embedded policy, SSH certificates for ecosystem fit}` in eidos/spec - edgrant protocol.md:37 — token format options, no decision needed yet
- `{>> same convention as EdProof's edproof namespace — consistent ecosystem}` in eidos/spec - edgrant protocol.md:90 — design rationale note

## Forward-Looking Opportunities

1. **Seed the eidos spec** — replace eidos/seed.md with project-specific content (the protocol spec is already pulled, but seed.md still has default template text)
2. **Move use cases into eidos/** — `spec - permission grant use cases.md` lives at project root, not in eidos/. Consider moving for spec graph consistency
3. **Spec the formal model** — the Tamarin model (formal/edgrant.spthy) is mapped from the protocol spec but has no dedicated eidos spec for its verification properties and modelling decisions
4. **Implementation planning** — no reference implementation exists yet. Could plan a minimal SDK or CLI tool
5. **Merge task branch** — task/eidos-init has 4 commits ready for main

## Todos

None.

## Goodjobs

None.

## Postponed

None.
